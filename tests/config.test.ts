import { describe,expect,it } from 'vitest'; import YAML from 'yaml'; import { readFileSync } from 'node:fs'; import { configSchema } from '../src/config.js';
describe('production config',()=>{it('has the required v0 cadence',()=>{const c=configSchema.parse(YAML.parse(readFileSync('config/production.yaml','utf8')));expect(c.platforms.threads.postsPerDay).toBe(5);expect(c.platforms.facebook.postsPerWeek).toBe(2);expect(c.platforms.instagram.postsPerWeek).toBe(1);expect(c.dispatcher.lookaheadHours).toBe(24);});});
