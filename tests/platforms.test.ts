import { beforeEach,describe,expect,it } from 'vitest'; import type { PreparedPost } from '../src/types.js'; import { classifyError,PlatformError } from '../src/platforms/base.js';
const p={id:'1',contentPlanId:'2',platform:'threads',copyText:'hello',scheduledFor:'',idempotencyKey:'k',campaignSlug:'c',claimManifest:[]} as PreparedPost;
describe('platform behavior',()=>{beforeEach(()=>{process.env.THREADS_ACCESS_TOKEN='secret';process.env.FACEBOOK_PAGE_ACCESS_TOKEN='secret';process.env.INSTAGRAM_ACCESS_TOKEN='secret';});it('classifies transient and permanent errors',()=>{expect(classifyError(new PlatformError('busy',503)).retryable).toBe(true);expect(classifyError(new PlatformError('forbidden',403)).retryable).toBe(false);expect(classifyError(new Error('timeout')).ambiguous).toBe(true);});it('keeps input immutable for payload construction',async()=>{const before=JSON.stringify(p);expect(JSON.stringify(p)).toBe(before);});});
