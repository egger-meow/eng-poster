import { env } from '../env.js'; import { MarketingRepository } from '../db/repository.js'; import { publisherFor } from '../platforms/index.js'; import type { Platform,TokenHealth } from '../types.js';
export async function tokenHealth():Promise<TokenHealth[]>{const repo=new MarketingRepository(),out:TokenHealth[]=[];for(const p of ['facebook','instagram','threads'] as Platform[]){if(!env[`${p.toUpperCase()}_ENABLED` as keyof typeof env])continue;try{const h=await publisherFor(p).validateCredentials();await repo.recordHealth(h);out.push(h);}catch(e){const h:TokenHealth={platform:p,valid:false,grantedScopes:[],diagnostic:e instanceof Error?e.message:'Unknown credential error'};await repo.recordHealth(h);out.push(h);}}if(out.some(h=>!h.valid))process.exitCode=1;return out;}
