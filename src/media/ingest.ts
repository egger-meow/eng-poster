import { readdir,readFile } from 'node:fs/promises'; import { basename,extname,join,relative } from 'node:path'; import YAML from 'yaml'; import { getSupabase } from '../db/client.js'; import { MarketingRepository } from '../db/repository.js'; import { newId,sha256 } from '../shared/hash.js'; import type { AssetRecord,Platform } from '../types.js'; import { inspectImage } from './validate.js';
async function walk(dir:string):Promise<string[]>{const out:string[]=[];for(const e of await readdir(dir,{withFileTypes:true})){const p=join(dir,e.name);if(e.isDirectory())out.push(...await walk(p));else if(/\.(png|jpe?g|webp)$/i.test(e.name))out.push(p);}return out;}
export async function ingestAssets(root='assets/manual'):Promise<number>{const repo=new MarketingRepository(),db=getSupabase();let count=0;for(const file of await walk(root)){const bytes=await readFile(file),meta=await inspectImage(bytes),hash=sha256(bytes),side=file.slice(0,-extname(file).length)+'.yaml';let md:any={};try{md=YAML.parse(await readFile(side,'utf8'))??{};}catch(e:any){if(e.code!=='ENOENT')throw e;}const storagePath=`manual/${hash.slice(0,2)}/${hash}-${basename(file)}`;const {error}=await db.storage.from('marketing-media').upload(storagePath,bytes,{contentType:meta.mime,upsert:false});if(error&&!/already exists|Duplicate/i.test(error.message))throw error;const publicUrl=db.storage.from('marketing-media').getPublicUrl(storagePath).data.publicUrl;const folder=relative(root,file).split(/[\\/]/)[0]??'manual';const asset:AssetRecord={id:newId(),source:folder==='product'?'screenshot':'manual',contentHash:hash,storagePath,publicUrl,width:meta.width,height:meta.height,format:meta.format,topics:md.topics??basename(file,extname(file)).split(/[-_ ]+/),audience:md.audience??[],allowedPlatforms:(md.platforms??['facebook','instagram','threads']) as Platform[],reuse:md.reuse??true,priority:md.priority??0,concept:md.concept??null,expiresAt:md.expires_at??null,usageCount:0,lastUsedAt:null};await repo.upsertAsset(asset);count++;}return count;}
