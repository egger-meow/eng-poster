import { structured } from '../ai/openai.js'; import type { Claim, Platform, ResearchSnapshot } from '../types.js';
export interface GeneratedVariant{platform:Platform;copyText:string;claims:Claim[];needsMedia:boolean;visualConcept:string}
const schema={type:'object',properties:{variants:{type:'array',items:{type:'object',properties:{platform:{type:'string',enum:['facebook','instagram','threads']},copyText:{type:'string'},claims:{type:'array',items:{type:'object',properties:{text:{type:'string'},kind:{type:'string',enum:['brand_fact','researched_fact','opinion','rhetorical']},sourceUrls:{type:'array',items:{type:'string'}}},required:['text','kind','sourceUrls'],additionalProperties:false}},needsMedia:{type:'boolean'},visualConcept:{type:'string'}},required:['platform','copyText','claims','needsMedia','visualConcept'],additionalProperties:false}}},required:['variants'],additionalProperties:false};
export async function generateVariants(input:{topic:string;archetype:string;research:ResearchSnapshot;brand:string;product:string;claims:string;voice:string;model:string}):Promise<GeneratedVariant[]>{const r=await structured<{variants:GeneratedVariant[]}>({model:input.model,name:'platform_variants',schema,instructions:'Write independent Traditional Chinese copy for Facebook, Instagram, and Threads. Aggressive hook, conservative truth. Do not invent claims. Every factual claim must map to a research URL or checked-in brand evidence marker knowledge://brand or knowledge://product.',input:JSON.stringify(input)});return r.variants;}
